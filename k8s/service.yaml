apiVersion: v1
kind: Service
metadata:
  name: react-app-service
  namespace: qa
spec:
  type: LoadBalancer
  selector:
    app: react-app
  ports:
  - protocol: TCP
    port: 80         # Puerto expuesto al público (HTTP)
    targetPort: 80   # Puerto del contenedor Nginx
